# 虚拟博弈（FP）反馈学习机制设计方案

## 🎯 核心思路

**设计目标**：
1. 让LLM明确知道目标是**利益最大化**（净收益 = 补偿 - 隐私成本）
2. 每轮提供足够信息，让LLM**自己推理**上一轮决策的好坏
3. 不直接说"你做得好/不好"，而是给数据让他反思

**关键原则**：
- ✅ 隐式引导，而非明确告知
- ✅ 提供原始数据，让LLM自己判断
- ✅ 强化目标函数意识（净收益最大化）
- ✅ 避免变成"告诉答案"的监督学习

---

## 💡 方案A：隐式反事实（推荐指数：⭐⭐⭐⭐）

### 设计思路
提供完整的历史决策记录和实际结果，让LLM自己对比"预期 vs 实际"，自行推断决策质量。

### 提示词示例

```markdown
## 📋 你的历史决策记录

### 第3轮回顾
- **你的选择**：分享
- **你的预期**：约3-4人会分享
- **实际结果**：5人分享（用户{0,2,5,7,8}）
- **你获得补偿**：p = 0.45

### 第2轮回顾
- **你的选择**：不分享  
- **你的预期**：约2人会分享
- **实际结果**：2人分享（用户{0,5}）
- **你获得补偿**：0

### 第1轮回顾
- **你的选择**：分享
- **你的预期**：约3人会分享
- **实际结果**：3人分享（用户{0,2,5}）
- **你获得补偿**：p = 0.45

---

💭 **反思提示**：
- 回顾你的选择：给定实际参与人数，你的边际隐私成本是多少？
- 你的净收益是多少？（补偿 - v×边际泄露）
- 如果重来，你会做同样的选择吗？

---

🎯 **你的目标：最大化净收益**

净收益 = 补偿收益 - 隐私成本
       = p（如果分享）或 0（如果不分享） - v×边际泄露

基于历史经验调整策略：
1. 你的预期是否准确？
2. 你的选择在实际环境下是否最优？
3. 如何改进你的决策？
```

### 优点
- ✅ 提供原始数据，让LLM自己计算
- ✅ 不直接评判，符合"让他自己理解"的要求
- ✅ 强化目标函数意识
- ✅ 帮助LLM校准预期（预期 vs 实际）

### 缺点
- ❌ LLM可能算不准边际泄露（需要复杂推理）
- ❌ 提示词较长，可能增加成本

### 实现要点
- 需要记录每轮LLM的"预期参与人数"（从reason中提取）
- 展示最近3-5轮即可，避免过长
- 关键是提供"预期 vs 实际"的对比

---

## 💡 方案B：简化版 - 只展示收益对比（推荐指数：⭐⭐⭐）

### 设计思路
简化信息，只展示核心数据：选择、环境、收益。降低LLM计算负担，聚焦在"收益-环境"关系上。

### 提示词示例

```markdown
## 📊 你的历史表现

| 轮次 | 你的选择 | 实际参与人数 | 你获得补偿 |
|------|---------|-------------|-----------|
| 第3轮 | 分享    | 5人         | +0.45     |
| 第2轮 | 不分享  | 2人         | 0         |
| 第1轮 | 分享    | 3人         | +0.45     |

**累计补偿**：0.90

---

💡 **思考要点**：
- 给定实际参与人数，你的选择是否最大化了净收益？
- 参与人数少时分享 vs 参与人数多时不分享，哪个更划算？
- 你的v = 0.85，在不同环境下，补偿p = 0.45是否足够覆盖边际成本？

---

🎯 **你的目标：最大化净收益**

净收益 = 补偿 - v×边际泄露

- **参与人数多**：边际泄露小，分享的成本低
- **参与人数少**：边际泄露大，分享的成本高

思考：在什么环境下，p = 0.45 能覆盖你的边际成本？
```

### 优点
- ✅ 更直观，降低计算负担
- ✅ 让LLM关注"补偿"vs"环境"的关系
- ✅ 表格形式清晰易读

### 缺点
- ❌ 只有收益，没有成本，可能引导多分享
- ❌ 需要LLM自己推断隐私成本
- ❌ 缺少"预期"信息，难以学习预测能力

### 实现要点
- 只需记录基本信息，实现简单
- 可在"思考要点"中给一些引导性问题
- 展示累计补偿，但不要过度强调（避免引导多分享）

---

## 💡 方案C：反事实明示（推荐指数：⭐⭐）

### 设计思路
明确展示"你选了A，如果选B会怎样"的对比，提供最直接的学习信号。

### 提示词示例

```markdown
## 📋 第3轮决策分析

**你选择了**：分享  
**实际环境**：5人分享

### 你的结果
- 收益：+0.45
- 成本：v×边际泄露（5人环境下的边际）
- 净收益：p - v×边际泄露

### 反事实：如果你选择不分享
- 收益：0
- 成本：v×0 = 0
- 净收益：0

---

💭 **思考**：哪个选择的净收益更高？

---

（对第2轮、第1轮做类似展示）
```

### 优点
- ✅ 最明确的学习信号
- ✅ 直接对比两种选择，容易理解

### 缺点
- ❌ 太明确，可能变成"告诉答案"
- ❌ 需要我们计算边际泄露（复杂，且可能不准）
- ❌ 失去了"让他自己判断"的设计初衷

### 实现要点
- 需要我们计算每轮的边际泄露量
- 实现复杂，且可能引入计算误差
- **不推荐**：过于明确，违背隐式引导原则

---

## 💡 方案D：RL风格奖励信号（推荐指数：⭐⭐⭐⭐⭐）

### 设计思路
给一个**方向性反馈**，但不说绝对好坏。帮助LLM建立"环境-决策"的关联，同时保持足够的隐式性。

### 提示词示例

```markdown
## 📊 历史决策与环境反馈

### 第3轮
- **你选了**：分享
- **实际环境**：5人分享（高参与）
- **你获得**：+0.45补偿
- **环境特征**：边际成本较低（因为参与人数多）

### 第2轮
- **你选了**：不分享
- **实际环境**：2人分享（低参与）
- **你获得**：0
- **环境特征**：边际成本较高（因为参与人数少）

### 第1轮
- **你选了**：分享
- **实际环境**：3人分享（中等参与）
- **你获得**：+0.45补偿
- **环境特征**：边际成本中等

---

💡 **学习要点**：
- **高参与环境**（4+人）：边际成本低，分享可能划算
- **低参与环境**（0-2人）：边际成本高，不分享可能划算
- **中等参与环境**（3人）：需要仔细权衡 p vs v×边际泄露

💭 **反思**：
- 回顾你的历史选择，在什么环境下你选择分享？在什么环境下选择不分享？
- 给定你的v = 0.85和p = 0.45，什么样的参与人数是你的"临界点"？
- 你的预期是否与实际环境一致？如何改进预测？

---

🎯 **你的目标：最大化净收益**

净收益 = 补偿 - v×边际泄露

**决策公式**：
- 如果 p > v×边际泄露 → 分享更优
- 如果 p < v×边际泄露 → 不分享更优

**关键**：准确预测参与人数，从而估计边际泄露
```

### 优点
- ✅ 给"方向性"反馈，不给绝对评价
- ✅ 帮助LLM建立环境-决策的关联（高/中/低参与）
- ✅ 符合"让他自己判断"的原则
- ✅ 引入"临界点"思维，促进深度理解
- ✅ 平衡性好：既有引导，又不是告诉答案

### 缺点
- ❌ "环境特征"的提示可能还是有点明确
- ❌ 需要定义"高/中/低参与"的阈值

### 实现要点
- 根据实际参与人数分类环境：
  - 低参与：0-2人
  - 中等参与：3-4人
  - 高参与：5+人
- 只给环境特征描述（边际成本高/低），不给具体数值
- 关键是提出引导性问题，让LLM自己思考

---

## 📊 方案对比总结

| 方案 | 信息量 | 隐式程度 | 实现难度 | 学习效果 | 推荐指数 |
|------|--------|---------|---------|---------|---------|
| A. 隐式反事实 | 高 | 高 | 中 | 强（需LLM计算能力） | ⭐⭐⭐⭐ |
| B. 收益对比 | 低 | 中 | 低 | 中（可能偏向分享） | ⭐⭐⭐ |
| C. 反事实明示 | 极高 | 低 | 高 | 强（但太明确） | ⭐⭐ |
| D. RL风格 | 中高 | 中高 | 中低 | 强（平衡最好） | ⭐⭐⭐⭐⭐ |

---

## 🎖️ 最终推荐：方案D（RL风格）的简化实现

### 核心设计

1. **历史回顾部分**（每轮展示）：
   ```
   你的上N轮决策：
   - 第X轮：选择Y，实际环境Z人，获得补偿W，环境特征：...
   ```

2. **学习提示**（引导思考）：
   ```
   学习要点：
   - 高/中/低参与环境的特点
   - 反思你的选择模式
   - 思考你的临界点
   ```

3. **目标强化**（每轮重复）：
   ```
   你的目标：最大化净收益 = p - v×边际泄露
   ```

### 需要记录的信息
- 每轮的选择（0/1）
- 实际参与人数
- 获得的补偿（0或p）
- （可选）LLM的预期参与人数

### 展示规则
- 展示最近**3-5轮**（信念窗口大小）
- 按时间倒序（最近的在前）
- 简洁呈现，避免过长

---

## ❓ 实现前需要确认的问题

### 1. 反馈粒度
- **选项A**：只回顾上一轮
- **选项B**：展示最近3轮
- **选项C**：展示最近5轮（与belief_window一致）
- **推荐**：选项C，与信念窗口保持一致

### 2. 计算帮助
- **选项A**：不给任何计算帮助，完全让LLM自己推断
- **选项B**：给环境分类（高/中/低参与 → 边际成本低/中/高）
- **选项C**：给具体的边际泄露估算值（需要我们计算）
- **推荐**：选项B，平衡隐式性和可理解性

### 3. 对比方式
- **选项A**：只展示实际结果，不提反事实
- **选项B**：隐晦提示"想想另一个选择会怎样"
- **选项C**：明确展示反事实对比
- **推荐**：选项A，完全隐式

### 4. 目标强调
- **选项A**：只在第一轮强调目标
- **选项B**：每轮都重复目标函数
- **推荐**：选项B，持续强化目标意识

### 5. 预期记录
- **选项A**：从LLM的reason中提取预期参与人数（复杂）
- **选项B**：不记录预期，只展示实际（简单）
- **推荐**：选项B（初期），可在后续迭代中尝试选项A

---

## 🚀 实现计划

### Phase 1：基础版本
- 记录：选择、实际参与人数、补偿
- 展示：最近3-5轮的基本信息
- 强化：每轮重复目标函数

### Phase 2：增强版本（可选）
- 添加：环境分类（高/中/低参与）
- 添加：学习提示和引导问题
- 添加：临界点思维引导

### Phase 3：高级版本（可选）
- 提取：LLM的预期参与人数
- 对比：预期 vs 实际
- 引导：预测能力校准

---

## 📝 代码实现建议

### 需要修改的函数
```python
def build_user_decision_prompt_fp(
    self, 
    user_id: int, 
    price: float, 
    history: List[Dict[int, int]], 
    belief_probs: Dict[int, float], 
    current_round: int,
    # 新增参数：
    user_decision_history: List[Dict]  # 该用户的历史决策记录
) -> str:
```

### user_decision_history 结构
```python
[
    {
        "round": 3,
        "choice": 1,  # 0或1
        "actual_participants": 5,
        "compensation": 0.45,
        # 可选：
        "expected_participants": 3.5  # 从reason提取
    },
    ...
]
```

### 在 simulate_fictitious_play 中记录
```python
# 为每个用户维护决策历史
user_histories = {uid: [] for uid in range(n)}

for round_idx in range(max_rounds):
    for user_id in range(n):
        # 查询决策
        decision = query_user_decision_fp(
            ..., 
            user_decision_history=user_histories[user_id]
        )
        
        # 记录决策
        user_histories[user_id].append({
            "round": round_idx + 1,
            "choice": decision,
            "actual_participants": None,  # 先占位
            "compensation": price if decision == 1 else 0
        })
    
    # 本轮结束后，更新actual_participants
    actual_count = sum(decisions.values())
    for user_id in range(n):
        user_histories[user_id][-1]["actual_participants"] = actual_count
```

---

## 💭 设计哲学

**关键平衡**：
- **太隐式** → LLM学不到东西
- **太明确** → 变成监督学习，失去探索

**我们的选择**：
- 提供充分的**原始数据**（选择、环境、补偿）
- 给予适度的**引导问题**（不是答案）
- 强化明确的**目标函数**（净收益最大化）
- 让LLM**自己推理**（好/坏，为什么，怎么改进）

**本质**：这不是强化学习（没有奖励函数），而是**增强的虚拟博弈**——在经验频率的基础上，加入对自身历史表现的反思。
